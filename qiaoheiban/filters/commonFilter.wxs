var commonFilter = {
  // 转换课程状态：0-》正常上课；1-》表示试听； 2-》表示请假
  converCourseStatus: function (originValue) {
    console.log(123);
    var targetVaule = '';
    targetVaule = originValue == 0 ? '' : originValue == 1 ? '试听' : '请假';
    return targetVaule;
  }
}
module.exports = commonFilter;